# 인수인계 메모 (handoff.md)

> 다음에 이어서 작업할 사람(또는 Claude)에게 넘기는 "지금까지 뭐 했고, 다음에 뭐 하면 되는지" 메모입니다.
> **최종 수정: 2026-07-29**

## 한 줄 요약

시장 대시보드에 **나스닥(NASDAQ)** 및 **QLD(2배 레버리지 ETF)**를 추가하고, 6개 카드를 **3x2 레이아웃**으로 재배치하였으며, 고점 대비 하락률의 시인성을 극대화하기 위해 미니 배지 스타일을 적용했습니다. 또한, yfinance 차단으로 인한 로딩 지연 현상을 백그라운드 비동기 스레드로 완벽하게 해결했습니다.

## 이번에 한 일 (완료)

- **나스닥(NASDAQ) 및 QLD 추가**:
  - `market_data.py`에 나스닥 종합 지수(`^IXIC`)와 2배 레버리지 ETF인 QLD(`QLD`)를 추가했습니다.
  - 전고점(ATH) 기본값을 각각 `27190.21`(나스닥) 및 `105.00`(QLD)으로 설정하여 수치 왜곡을 바로잡았습니다.
  - 네이버 금융 비공식 지수 API(`.IXIC` 코드) 및 KIS ETF 지수 변환용 스케일링 계수(`36.75`배)를 추가 연동했습니다.
- **대시보드 3x2 그리드 레이아웃 개편**:
  - 카드가 늘어남에 따라 발생하던 가로 찌그러짐 현상을 해결하기 위해 가로 3열, 세로 2행의 반응형 Grid 레이아웃으로 변경했습니다.
  - **1행**: 코스피, 코스닥, S&P 500 / **2행**: 나스닥, QLD, TQQQ
- **고점 대비 하락률 시인성 개선**:
  - 대시보드 내 고점 대비 하락률을 강조하기 위해 폰트 굵기를 `800`으로 높이고, 텍스트 뒤에 붉은색/파란색의 반투명 배경 배지(Badge) 형태를 씌워 한눈에 보이도록 디자인을 강화했습니다.
- **시세 수집 로딩 지연 해결 (비동기 최적화)**:
  - 서버 환경에서 yfinance API가 차단되면 30일치 기록 수집 시 최대 25초까지 지연이 발생해 Playwright 캡처 타임아웃(15초 초과) 에러가 발생하던 현상을 개선했습니다.
  - 스파크라인 수집 로직(`_fetch_sparklines()`)을 **비동기 백그라운드 스레드(Thread)**로 구동시켜 캐시가 비어 있거나 만료되었을 때도 API 응답이 `0.5초 이내`에 즉시 반환되도록 극적으로 최적화했습니다.
  - 한국투자증권 및 yfinance가 둘 다 차단되는 상황을 대비해 국내 지수(코스피/코스닥)도 네이버 금융 API로 안전하게 백업 시세를 가져오도록 예외 처리를 확장했습니다.

## 작업 중 발생했던 주요 이슈 및 문제 해결 (Troubleshooting)

### 1. 포트 충돌 및 옛날 화면 캡처 문제
* **현상**: 코드를 분명히 업데이트했는데도 텔레그램 사진에는 옛날 4개 카드 화면과 잘못된 최고가(KOSPI 9385.59 등)가 계속 찍혀서 날아왔습니다.
* **원인**: 백그라운드(8000 포트)에 이전 버전 코드로 작동 중인 오래된 파이썬 프로세스가 살아 있었기 때문입니다. 이로 인해 Playwright가 8000번 포트에 접속했을 때 옛날 서버의 화면이 캡처되었습니다.
* **해결**: 관리자 권한으로 기존 파이썬 프로세스들을 완전히 킬(`sudo pkill -f "python"`)한 후에 테스트를 실행하여 해결했습니다.

### 2. 서버 Git remote 주소 불일치
* **현상**: 서버 터미널에서 `git pull` 시 `master` 브랜치를 찾을 수 없거나 remote refs를 읽어오지 못하는 에러가 발생했습니다.
* **원인**: 서버 내 `~/stock/stock` 폴더의 git remote 주소가 로컬 맥의 `stock.git`이 아닌 `openclaw.git` 주소로 꼬여 있었습니다.
* **해결**: 서버에서 원격지 주소를 명시적으로 재설정하여 해결했습니다.
  ```bash
  git remote set-url origin https://github.com/richdadtechtree/stock.git
  git pull origin master
  ```

### 3. 우분투 환경 명령어 에러
* **현상**: `python scheduler.py` 실행 시 `python command not found` 에러가 나거나 가상환경을 찾지 못했습니다.
* **해결**: 프로젝트 폴더로 이동(`cd ~/stock/stock`)하여 전용 가상환경(`source venv/bin/activate`)을 켠 후 `python` 명령어를 실행하거나, `python3` 명령어를 직접 사용해 실행해 주어야 합니다.

## 서버 터미널에서의 실행 및 업데이트 가이드

수정사항 반영 및 테스트 보고서를 즉시 발송하고 싶다면 서버 터미널에서 아래 순서대로 실행하십시오.

```bash
# 1. 프로젝트 폴더로 이동 및 가상환경 활성화
cd ~/stock/stock
source venv/bin/activate

# 2. 최신 코드 풀(Pull)
git remote set-url origin https://github.com/richdadtechtree/stock.git
git pull origin master

# 3. 데이터베이스 상태 캐시 리셋 (QLD 전고점 데이터 갱신)
rm alert_state.db 2>/dev/null || true

# 4. 기존 프로세스 완전 종료
sudo pkill -f "python"

# 5. 즉시 테스트 발송 실행 (대시보드 이미지 캡처 후 텔레그램으로 즉시 보고)
python scheduler.py --test
```

## 알아두면 좋은 점

- QLD는 2배 레버리지 ETF 특성상 미국 정규장 시간에 전고점을 넘으면 자동으로 `alert_state.db`에 새 ATH로 기록 및 갱신됩니다. (초기 세팅 ATH는 `105.00` 기준)
- 스파크라인 로드가 백그라운드 비동기로 가동되므로 첫 캡처 시 그래프가 평평한 직선으로 나타날 수 있으나, 서버가 켜진 상태로 백그라운드 갱신이 완료되면 다음 캡처부터는 정상 그래프가 채워져서 보입니다.
